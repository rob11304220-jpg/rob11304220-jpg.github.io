---
interface Props {
	title: string;
	description: string;
	href: string;
}

const { title, description, href } = Astro.props;
---

<li class="project-card surface">
	<h3>
		<a href={href}>{title}</a>
	</h3>
	<p>{description}</p>
</li>
