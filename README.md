# Personal homepage

Static site built with [Astro](https://astro.build). Live site: https://rob11304220-jpg.github.io

## Commands

```bash
npm install
npm run dev
npm run build
```

GitHub Actions builds on every push to `main` and publishes to GitHub Pages. In the repo **Settings → Pages**, set the source to **GitHub Actions** if it is not already.
